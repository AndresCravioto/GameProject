class Weapon {
    constructor(type = 'PILAR OF FLAME') {
        this.type = type;
    };
};


/*
let tiles;

function load1() {

	let image = new Image();
	
    image.addEventListener("load", function(event) {
		tiles = this;

    });
    image.src = "GameTiles.png";

};

load1();

              context.drawImage(tiles, 0, 0, tileWidth, tileHeight, x*tileWidth, y*tileHeight, tileWidth, tileHeight);
              
              */